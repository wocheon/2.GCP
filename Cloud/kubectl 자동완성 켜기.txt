1.
vi ~/.bashrc > 마지막 3줄 주석처리 해제 

echo "source <(kubectl completion bash)" >> ~/.bashrc

source ~/.bashrc 

2. 
vi /etc/profile
kubectl completion bash
source /etc/profile

3. 영구적용
kubectl completion bash | sudo tee /etc/bash_completion.d/kubectl > /dev/null
